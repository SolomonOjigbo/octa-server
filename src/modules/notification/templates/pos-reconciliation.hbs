<!DOCTYPE html>
<html>
<head>
    <title>Cash Reconciliation - #{{id}}</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { text-align: center; margin-bottom: 20px; }
        .details { margin-bottom: 30px; }
        .footer { margin-top: 30px; text-align: center; color: #777; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Cash Reconciliation</h1>
        </div>
        
        <div class="details">
            <p><strong>Reconciliation ID:</strong> {{id}}</p>
            <p><strong>Session ID:</strong> {{sessionId}}</p>
            <p><strong>Expected Cash:</strong> ₦{{formatCurrency expectedCash}}</p>
            <p><strong>Actual Cash:</strong> ₦{{formatCurrency actualCash}}</p>
            <p><strong>Variance:</strong> ₦{{formatCurrency variance}}</p>
            <p><strong>Reason:</strong> {{varianceReason}}</p>
            <p><strong>Performed By:</strong> {{createdBy.name}}</p>
            <p><strong>Date:</strong> {{formatDate createdAt}}</p>
        </div>
        
        <div class="footer">
            <p>This reconciliation has been recorded in our system</p>
        </div>
    </div>
</body>
</html>